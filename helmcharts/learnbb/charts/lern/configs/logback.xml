<configuration>

  <conversionRule conversionWord="coloredLevel" converterClass="play.api.libs.logback.ColoredLevel" />

  <!-- transaction-event-trigger START -->
  <timestamp key="timestamp" datePattern="yyyy-MM-dd"/>
  <!-- common transactions logs -->
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%date{yyyy-MM-dd HH:mm:ss} %coloredLevel %logger{15} - [%thread] %message%n%xException</pattern>
    </encoder>
  </appender>

  <appender name="JSONSTDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder class="net.logstash.logback.encoder.LogstashEncoder">
        <includeContext>true</includeContext>
        <includeCallerData>false</includeCallerData>
        <customFields>{"app":"sunbird-learning-service"}</customFields>
    </encoder>
  </appender>

  <appender name="ASYNCJSONSTDOUT" class="ch.qos.logback.classic.AsyncAppender">
    <appender-ref ref="JSONSTDOUT" />
  </appender>

  <appender name="queryLoggerAppender" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
        <pattern>%date{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <appender name="defaultLoggerAppender" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
        <pattern>%date{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <appender name="kafka-appender" class="com.github.danielwegener.logback.kafka.KafkaAppender">
    <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
      <pattern>%msg</pattern>
    </encoder>

    <topic>${ENV_NAME}.telemetry.raw</topic>
    <!-- ensure that every message sent by the executing host is partitioned to the same partition strategy -->
    <keyingStrategy class="com.github.danielwegener.logback.kafka.keying.NoKeyKeyingStrategy" />
    <!-- block the logging application thread if the kafka appender cannot keep up with sending the log messages -->
    <deliveryStrategy class="com.github.danielwegener.logback.kafka.delivery.AsynchronousDeliveryStrategy" />

    <!-- each <producerConfig> translates to regular kafka-client config (format: key=value) -->
    <!-- producer configs are documented here: https://kafka.apache.org/documentation.html#newproducerconfigs -->
    <!-- bootstrap.servers is the only mandatory producerConfig -->
    <producerConfig>bootstrap.servers=${SUNBIRD_KAFKA_URL}</producerConfig>
    <!-- don't wait for a broker to ack the reception of a batch.  -->
    <producerConfig>acks=0</producerConfig>
    <!-- wait up to 1000ms and collect log messages before sending them as a batch -->
    <producerConfig>linger.ms=15000</producerConfig>
    <!-- even if the producer buffer runs full, do not block the application but start to drop messages -->
    <producerConfig>max.block.ms=0</producerConfig>
    <!-- define a client-id that you use to identify yourself against the kafka broker -->
    <producerConfig>client.id=${HOSTNAME}-${CONTEXT_NAME}-logback-relaxed</producerConfig>

    <!-- there is no fallback <appender-ref>. If this appender cannot deliver, it will drop its messages. -->

  </appender>

  <logger name="play" level="INFO" />
  <logger name="defaultLoggerAppender" level="INFO" />
  
  <!-- Noise Reduction -->
  <logger name="org.sunbird.logging.PrintEntryExitLog" level="WARN" />
  <logger name="org.sunbird.cassandra.CassandraUtil" level="WARN" />
  <logger name="org.sunbird.cassandra.CassandraDACImpl" level="WARN" />
  <logger name="org.sunbird.keycloak.impl.KeyCloakServiceImpl" level="WARN" />
  <logger name="org.sunbird.util.Util" level="WARN" />
  <logger name="org.sunbird.telemetry.util.TelemetryEvents" level="WARN" />
  <logger name="org.sunbird.telemetry.util.TelemetryWriter" level="WARN" />
  <logger name="org.sunbird.common.CassandraUtil" level="WARN" />
  
  <!-- Telemetry Loggers-->
  <logger name="TelemetryEventLogger" level="INFO">
    <appender-ref ref="kafka-appender" />
  </logger>

  <logger name="queryLogger" level="DEBUG">
    <appender-ref ref="defaultLoggerAppender" />
  </logger>

  <root level="INFO">
    <appender-ref ref="ASYNCJSONSTDOUT" />
  </root>

</configuration>